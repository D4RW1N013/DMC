# Installation Instructions

1. Ensure Git is installed: https://git-scm.com/
2. Clone the repository: `git clone https://github.com/username/DMC.git`
3. Navigate to the directory: `cd DMC`
4. Install dependencies: `npm install` (or relevant package manager)
5. Configure environment variables in `.env` file.